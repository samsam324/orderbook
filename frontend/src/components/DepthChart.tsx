/** Owner: ROLE 5 (UI + Backend WS API) */

export function DepthChart() {
  // Optional component per spec
  // TODO(ROLE5): Add a small depth / spread visualization once snapshot schema exists
  return (
    <div className="panel">
      <div className="panelHeader">
        <div>Depth Chart</div>
        <div className="text-xs font-normal text-neutral-400">optional</div>
      </div>
      <div className="panelBody text-sm text-neutral-400">Not implemented.</div>
    </div>
  );
}

